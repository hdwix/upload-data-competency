export enum EmailType {
  REMINDER = 'reminder',
  ORDINARY = 'ordinary',
  CALENDAR = 'calendar',
  REMINDER_IMMEDIATELY = 'reminder_immediately',
}

export enum EmailStatus {
  PENDING = 'pending',
  SEND = 'send',
}